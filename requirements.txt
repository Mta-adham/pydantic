# Pydantic benchmark hub — Python dependencies (requires Python 3.12+).
#
#   python3.12 -m venv .venv && .venv/bin/pip install -r requirements.txt
#   source .venv/bin/activate

gsobench>=0.1.7
PyYAML>=6.0
