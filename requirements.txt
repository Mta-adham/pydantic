# Pydantic benchmark hub — Python dependencies.
#
# Setup (from this directory):
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# Inside the gso monorepo (repos/pydantic/), also install the parent package
# editable so ./pydantic uses local scripts and examples/local_patch_workflow.py:
#   pip install -e ../..

gsobench>=0.1.7
PyYAML>=6.0
